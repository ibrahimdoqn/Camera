PyQt6>=6.6.0
numpy>=1.26.0
onvif-zeep>=0.2.12
psutil>=5.9.0
# python-vlc is the sole video + audio backend. libVLC handles the RTSP
# client, the codec pipeline and (crucially) the hardware-accelerated
# decode path — DXVA2 / D3D11 on Windows, VAAPI on Linux, VideoToolbox
# on macOS, plus NVDEC (via ``--avcodec-hw=nvdec``) on NVIDIA GPUs and
# Quick Sync on Intel iGPUs. Requires VLC to also be installed
# system-wide (https://www.videolan.org/vlc/) in the same 32/64-bit
# architecture as the Python interpreter, so libvlc.dll is reachable.
python-vlc>=3.0.0
# opencv-python is optional. It's only used as a fallback for GPU
# detection (cv2.cuda.getCudaEnabledDeviceCount) when ``nvidia-smi`` is
# not on PATH — the app runs fine without it.
opencv-python>=4.9.0
